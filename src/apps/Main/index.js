import React, { Component } from 'react'
import {Jumbotron} from "reactstrap";

// app component
class Main extends Component {
    // render
    render () {
        return (<Jumbotron> This is in construction </Jumbotron>)
    }
}

export default Main
